import type { GameMode } from '@hokm/game-engine';

/** Short, plain-language rules for each variant, shown from the lobby. */
const GUIDES: Record<GameMode, { title: string; steps: string[]; notes: string[] }> = {
  classic4: {
    title: 'حکم چهار نفره چطور بازی می‌شود؟',
    steps: [
      'چهار نفر دو به دو یار می‌شوند: صندلی‌های ۱ و ۳ در برابر ۲ و ۴.',
      'به هر نفر ۵ کارت داده می‌شود و حاکم خال حکم را انتخاب می‌کند.',
      'بعد بقیه کارت‌ها پخش می‌شود تا هر نفر ۱۳ کارت داشته باشد.',
      'هر دور یک کارت می‌گذاری؛ باید همان خالِ زمینه را بازی کنی.',
      'اگر آن خال را نداری، می‌توانی با حکم ببری یا کارت دیگری رد کنی.',
      'هر تیمی زودتر ۷ دست بگیرد، برنده آن راند است.',
    ],
    notes: [
      'کوت: اگر حریف حتی یک دست هم نبرد، ۲ امتیاز می‌گیری.',
      'حاکم‌کوت: اگر تیم حاکم کوت شود، ۳ امتیاز به حریف می‌رسد.',
    ],
  },
  solo3: {
    title: 'حکم سه نفره چطور بازی می‌شود؟',
    steps: [
      'سه نفر بازی می‌کنند و هرکس برای خودش امتیاز جمع می‌کند؛ یار وجود ندارد.',
      'یک کارت ۲ از دسته حذف می‌شود تا ۵۱ کارت بین سه نفر بخش شود.',
      'به هر نفر ۵ کارت داده می‌شود و حاکم حکم را انتخاب می‌کند.',
      'بعد سه دور ۴تایی پخش می‌شود تا هر نفر ۱۷ کارت داشته باشد.',
      'قوانین بازی کردن دقیقاً مثل حکم چهار نفره است.',
      'هرکس زودتر ۷ دست بگیرد، برنده آن راند است.',
    ],
    notes: [
      'کارتی که حذف می‌شود هیچ‌وقت از خال حکم نیست.',
      'چون هرکس تنهاست، مراقب باش دو نفر دیگر با هم جلویت را نگیرند.',
    ],
  },
  duel2: {
    title: 'حکم دو نفره چطور بازی می‌شود؟',
    steps: [
      'فقط دو نفر بازی می‌کنند و هرکس برای خودش بازی می‌کند.',
      'به هر نفر ۵ کارت داده می‌شود و حاکم خال حکم را انتخاب می‌کند.',
      'بعد هر دو نفر ۲ کارت بی‌ارزش خود را می‌سوزانند (حریف آن‌ها را نمی‌بیند).',
      'حالا نوبتی از دسته وسط کارت برمی‌دارید تا هرکدام ۱۳ کارت داشته باشید.',
      'وقتی کارت‌ها کامل شد، بازی مثل حکم معمولی انجام می‌شود.',
      'هرکس زودتر ۷ دست بگیرد، برنده است.',
    ],
    notes: [
      'قانون مهم برداشتن: کارتی که رو می‌شود را می‌بینی. اگر نگهش داری، کارت بعدی نادیده سوزانده می‌شود.',
      'اگر بسوزانی‌اش، کارت بعدی را ندیده مجبوری برداری. این ریسک، هستهٔ بازی دو نفره است.',
      'کوت اینجا هم هست: اگر حریف هیچ دستی نبرد، ۲ امتیاز می‌گیری.',
    ],
  },
};

export function RulesGuide({ mode, onClose }: { mode: GameMode; onClose: () => void }) {
  const guide = GUIDES[mode] ?? GUIDES.classic4;

  return (
    <div className="rules-backdrop" role="dialog" aria-modal="true" aria-labelledby="rules-title">
      <div className="rules-sheet">
        <h2 id="rules-title">{guide.title}</h2>
        <ol className="rules-steps">
          {guide.steps.map((step) => (
            <li key={step}>{step}</li>
          ))}
        </ol>
        <ul className="rules-notes">
          {guide.notes.map((note) => (
            <li key={note}>{note}</li>
          ))}
        </ul>
        <button className="primary full-width" type="button" onClick={onClose}>
          فهمیدم
        </button>
      </div>
    </div>
  );
}
