# Keep package-lock.json stable on local Windows/dev machines.
# Use `npm install` for installing dependencies without rewriting the committed lockfile,
# so that `git pull origin main` does not conflict on package-lock.json.
package-lock=false
